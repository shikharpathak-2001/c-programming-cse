#include <stdio.h>

int main ()
{
    int num, digits=0;

    printf("Enter a Number :: ");
    scanf("%d", &num);

    while (num != 0)
    {
        num=num/10;
        digits++;
    }

    printf("Digits :: %d", digits);
}
