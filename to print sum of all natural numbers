#include<stdio.h>
int main()
{
    
    int number,x;
    unsigned int range;
    printf("enter range:");
    scanf("%d",&range);
for(number=1;number<=range;number++)
 x=number+x;
printf("%d",x);


return 0;
}
